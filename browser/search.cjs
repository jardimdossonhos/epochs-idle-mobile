const fs = require("fs"); const path = require("path"); function searchInDir(dir, regex) { const files = fs.readdirSync(dir); for (const file of files) { const fullPath = path.join(dir, file); const stat = fs.statSync(fullPath); if (stat.isDirectory()) { searchInDir(fullPath, regex); } else if (fullPath.endsWith(".ts") || fullPath.endsWith(".tsx")) { const content = fs.readFileSync(fullPath, "utf8"); const lines = content.split("\n"); lines.forEach((line, index) => { if (regex.test(line)) { console.log(`${fullPath}:${index + 1}: ${line.trim()}`); } }); } } } searchInDir(path.join(__dirname, "mobile", "src"), /taxPolicy|setTaxRate|dispatch|population/i);
