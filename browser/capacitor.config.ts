import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jardimdossonhos.epochsidlepc',
  appName: 'Epochs Idle',
  webDir: 'dist'
};

export default config;
