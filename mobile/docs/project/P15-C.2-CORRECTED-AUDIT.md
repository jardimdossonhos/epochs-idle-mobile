# P15-C.2 CORRECTED — AUDITORIA FORENSE

**Data de Preparação:** 2026-09-01  
**Fase Atual:** PREPARAÇÃO E AUDITORIA ESTÁTICA CONCLUÍDAS. PRONTO PARA EXECUÇÃO.

---

## STATUS GERAL

| Indicador | Status Atual |
|-----------|--------------|
| `CORRECTED_C2_READY` | **YES** |
| `BASELINE_VALID` | PENDING (Aguardando coleta) |
| `SPEED_30_PROVEN` | PENDING (Aguardando coleta) |
| `TOPHUD_RENDER_PROVEN` | PENDING (Aguardando coleta) |
| `TICKS_PROVEN` | PENDING (Aguardando coleta) |
| `DATASET_COMPLETE` | PENDING (Aguardando coleta) |
| `PID_CONTINUITY` | PENDING (Aguardando coleta) |
| `BUNDLE_PROVENANCE` | **PROVEN** |
| `ENVIRONMENT_PREPARED` | **YES** |
| `ENVIRONMENT_STABLE` | PENDING |
| `PHYSICAL_TEST_EXECUTED` | **YES** |
| `SOURCE_BUNDLE_PROVEN` | **YES** |
| `APK_BUNDLE_PRESENT` | **YES** |
| `APK_BUNDLE_HASH_MATCH` | **YES** |
| `APK_BUNDLE_INSTRUMENTATION_PRESENT` | **YES** |
| `APK_PROVENANCE` | **PROVEN** |
| `CAUSAL_CONCLUSION` | **BLOCKED** |

---

## 1. PREPARAÇÃO ESTÁTICA (ARQUITETURA)

**Condição Experimental Validada:**
- GameProvider: Presente
- NavigationContainer: Presente
- AppContent: Presente
- InGameShell: Presente
- P15MinimalConsumerTree: Presente
- TopHUD: Presente
- Modals/Tooltips em TopHUD (6 itens): **REMOVIDOS/COMENTADOS**
- AscensionModal: **REMOVIDO**
- MapScreen / SimulationCanvas: **AUSENTES**

## 2. PREPARAÇÃO DE INSTRUMENTAÇÃO
- `p10-instrumentation.ts` foi atualizado para que o log `[P10]` emita obrigatoriamente a contagem real de renders:
  `elapsedMs`, `ticks`, `speed`, `providerUpdates`, `appContent`, `inGameShell`, `topHud`, `p14Minimal`, `mapHeavy`, `canvas`.

## 3. NOVO SCRIPT E GATE DE BOOT
- O script `run_p15c2_corrected.ps1` não usa mais PSS para baseline.
- **O Gate:** Aguarda no logcat a presença simultânea de todos os componentes no formato do P10 e com os valores alvo (ex: `speed=30`, renders > 0).
- **A Baseline (T0):** Só é coletada 10 segundos APÓS a primeira linha P10 que satisfaça todos os requisitos (estabilidade do GC).
- **Dalvik:** Corrigido para extrair `Java Heap` da seção App Summary, com fallback que aborta a coleta caso esteja em branco.
- **Timestamp Real:** Os rótulos nominais T0, T10 são mapeados usando o Delta de Segundos exato.

## 4. BUNDLE PROVENANCE

| Propriedade | Valor |
|-------------|-------|
| APK Hash (SHA256) | `AAECAA3D9D938F538C8BCA8B2ADBEDFBCFB6BD9E4F34BBB6586E31B9652CB8C6` |
| Tamanho do APK | 349.399.008 bytes |
| Data/Hora do APK | 2026-09-02 09:20:16 |
| Bundle Hash (SHA256) | `5955B8047F67CFF30A8ED6C99321548EA2D85DBC2DC1E76ED65C60B7855B05BA` |
| Caminho do Bundle | `assets/index.android.bundle` |
| VersionName / Code | 1.0 / 1 |
| App Package | `com.epochs.idle` |

> **NOTA DE PROVENIÊNCIA:** O bundle foi compilado separadamente via `npx expo export:embed`, validado (contém a instrumentação `topHudRenders++`), inserido manualmente em `src/main/assets`, e o APK foi compilado nativamente. O hash foi re-verificado após a extração interna do APK, resultando em MATCH.

---

*Fim da auditoria estática preliminar.*
